{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install",
    "startCommand": "cd public && npm start"
  },
  "deploy": {
    "startCommand": "node server.js",
  }
}
